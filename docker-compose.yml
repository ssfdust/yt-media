version: '3.5'

services:  
    db:
        image: ccr.ccs.tencentyun.com/ssfdust/psql:latest
        environment:
            POSTGRES_USER: ${POSTGRES_USER:-smorest-admin}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-smorest2019}
            POSTGRES_DB: ${POSTGRES_DB:-smorest-production}
            PGDATA: /data/postgres

    redis:
        image: ccr.ccs.tencentyun.com/library/redis:latest
        command: redis-server --requirepass "smorest2019"

    rabbitmq:
        image: rabbitmq:latest
        environment:
            RABBITMQ_DEFAULT_USER: admin
            RABBITMQ_DEFAULT_PASS: admin
            RABBITMQ_DEFAULT_VHOST: smorest

    nginx:
        image: jwilder/nginx-proxy
        ports:
            - "80:80"
        volumes:
            - /var/run/docker.sock:/tmp/docker.sock:ro

    web:
        image: ccr.ccs.tencentyun.com/ssfdust/smart-smorest:master
        command: sh scripts/initapp.sh
        volumes:
            - .:/Application 
        restart: on-failure
        expose: 
            - "8000"
        depends_on:
            - db
            - rabbitmq
            - redis
        environment:
            - VIRTUAL_HOST=smorest.local
            - WEB_CONCURRENCY=2

    beat:
        image: ccr.ccs.tencentyun.com/ssfdust/smart-smorest:master
        command: celery beat --app=smorest_sfs.app:celery_app -S redbeat.RedBeatScheduler
        volumes:
            - .:/Application 
        depends_on:
            - web

    celery:
        image: ccr.ccs.tencentyun.com/ssfdust/smart-smorest:master
        command: celery --app=smorest_sfs.app:celery_app worker -l INFO -E
        volumes:
            - .:/Application 
        depends_on:
            - web

    pgadmin:
        image: dpage/pgadmin4
        expose: 
            - "80"
        environment:
            - VIRTUAL_HOST=pgadmin.local
            - PGADMIN_DEFAULT_EMAIL=admin@mail.com
            - PGADMIN_DEFAULT_PASSWORD=24WKisxian
